services:
  jenkins:
    image: jenkins/jenkins:lts
    ports: ["8080:8080"]

  sonarqube:
    image: sonarqube:community
    ports: ["9000:9000"]

  backend:
    build: ./backend
    ports: ["5000:5000"]

  frontend:
    build: ./frontend
    ports: ["3000:3000"]